# 撸货账本 · 长期公网部署（GitHub Pages）

部署完成后，你会得到一个固定网址，例如：

`https://你的用户名.github.io/luhuo-pwa/`

iPhone 用 Safari 打开 → **分享 → 添加到主屏幕** 即可长期使用。

---

## 第一步：注册 GitHub（已有账号可跳过）

1. 打开 https://github.com/signup 注册
2. 记住你的 **用户名**（例如 `zhangsan`）

---

## 第二步：在 GitHub 创建仓库

1. 登录 GitHub，右上角 **+** → **New repository**
2. 填写：
   - **Repository name**：`luhuo-pwa`（建议用这个名，方便记）
   - 选择 **Public**（公开，Pages 才免费）
   - **不要**勾选 "Add a README"
3. 点 **Create repository**
4. 创建后页面会显示仓库地址，形如：  
   `https://github.com/你的用户名/luhuo-pwa.git`

---

## 第三步：把本地项目推送到 GitHub

在 **PowerShell** 中依次执行（把 `你的用户名` 换成真实用户名）：

```powershell
cd C:\Users\zyl\Documents\luhuo-pwa

git init
git add .
git commit -m "deploy: 撸货账本 PWA"

git branch -M main
git remote add origin https://github.com/你的用户名/luhuo-pwa.git
git push -u origin main
```

第一次 `git push` 会弹出浏览器或窗口要求 **登录 GitHub**（可用账号密码或 Personal Access Token）。

> 若提示需要 Token：GitHub → Settings → Developer settings → Personal access tokens → 生成 token，勾选 `repo` 权限，推送时密码处粘贴 token。

---

## 第四步：开启 GitHub Pages

1. 打开仓库页面：`https://github.com/你的用户名/luhuo-pwa`
2. 点 **Settings** → 左侧 **Pages**
3. **Source** 选 **Deploy from a branch**
4. **Branch** 选 `main`，文件夹选 **`/ (root)`**
5. 点 **Save**
6. 等 1～3 分钟，页面顶部会出现绿色提示，访问地址为：  
   **`https://你的用户名.github.io/luhuo-pwa/`**

---

## 第五步：iPhone 添加到主屏幕

1. iPhone 用 **Safari**（不要用微信）打开上面的网址
2. 底部 **分享** 按钮 → **添加到主屏幕**
3. 名称可改为「撸货账本」→ 添加
4. 从主屏幕图标打开，即为全屏 App 体验

---

## 以后如何更新应用？

改完 `index.html` 后，在 PowerShell 执行：

```powershell
cd C:\Users\zyl\Documents\luhuo-pwa
git add .
git commit -m "update: 描述本次修改"
git push
```

等 1～2 分钟，手机 Safari **下拉刷新** 或关掉 App 重开即可看到更新。

---

## 手机还是旧页面？清除缓存

GitHub 更新后，iPhone 可能仍显示旧版（尤其从 **主屏幕图标** 打开时）。

### 方法一：Safari 直接打开（推荐先试）

1. **不要用主屏幕图标**，用 Safari 打开：  
   `https://yhzen.github.io/luhuo-pwa/?_=1`  
   （地址后加 `?_=1` 可绕过部分缓存）
2. 在页面底部点一下，看地址栏是否加载完成
3. 进入 **设置** → 若看到版本 **1.0.2** 且有 **「强制获取最新版」**，点一次
4. 确认正常后，可删掉旧的主屏幕图标，重新 **分享 → 添加到主屏幕**

### 方法二：清除该网站数据（彻底）

1. iPhone **设置** → **Safari** → **高级** → **网站数据**
2. 搜索 `github.io` 或 `luhuo`，左滑 **删除**
3. 再用 Safari 打开上面的网址

### 方法三：主屏幕 PWA

1. 删除桌面上旧的「撸货账本」图标
2. 按上文「添加到主屏幕」重新添加

> 清除网站数据 **不会删除** 已保存的订单（在 IndexedDB 里）；但若一直打不开 App，可先 **数据管理 → 备份 JSON** 再清缓存。

---

## 备选：Cloudflare Pages（不用 Git）

1. 注册 https://dash.cloudflare.com
2. **Workers & Pages** → **Create** → **Pages** → **Upload assets**
3. 把 `C:\Users\zyl\Documents\luhuo-pwa` 文件夹里的 `index.html` 打包成 zip 上传
4. 获得 `https://xxx.pages.dev` 地址，同样 Safari 添加到主屏幕

---

## 数据说明

- 订单数据存在 **每台手机自己的浏览器** 里，不会上传到 GitHub
- 换手机需要：旧手机 **数据管理 → 备份 JSON**，新手机 **恢复 JSON**
- 公网地址只负责提供 App 页面，不存储你的业务数据
